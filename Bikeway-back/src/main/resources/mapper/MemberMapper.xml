<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hulahoop.bikewayback.model.dao.MemberMapper">

    <resultMap id="memberLoginMap" type="com.hulahoop.bikewayback.model.dto.MemberLoginDTO">
        <result property="memberCode" column="member_code"/>
        <result property="id" column="id"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="login" resultMap="memberLoginMap">
        SELECT member_code, id, password
        FROM T_MemberInfo
        WHERE id = #{id} AND password = #{password}
    </select>

    <!-- ✅ 전화번호로 회원 코드 조회 -->
    <select id="findMemberCodeByPhone" resultType="Integer">
        SELECT member_code
        FROM T_MemberInfo
        WHERE phone_number = #{phone}
    </select>

</mapper>
